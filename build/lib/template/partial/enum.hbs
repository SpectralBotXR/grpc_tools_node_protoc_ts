
{{{indent}}}export enum {{{enumName}}} {
{{#each values}}
{{{indent}}}    {{{@key}}} = {{{this}}},
{{/each}}
{{{indent}}}{{curlyRight}}

{{!-- Maybe we can extend this to also export a Translations table if @Translation is configured in the extra settings of the Enum --}}